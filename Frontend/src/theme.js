import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FF8E53", // Orange
    },
    secondary: {
      main: "#ffffff", // White
    },
  },
});

export default theme;
